[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "smart-city-knowledge-graph"
version = "3.0.0"
requires-python = ">=3.12"

[tool.pytest.ini_options]
pythonpath = ["."]
